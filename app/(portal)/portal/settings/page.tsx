import { PortalPlaceholder } from '@/components/portal/placeholder';
export const metadata = { title: 'Settings - Aurelian Collective' };
export default function SettingsPage() {
  return <PortalPlaceholder icon="settings" title="Settings are coming soon" description="Account preferences, notification controls, and team access will live here." />;
}
